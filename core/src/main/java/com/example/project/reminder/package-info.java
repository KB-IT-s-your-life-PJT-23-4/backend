/**
 * Reminder domain.
 *
 * <p>신고기한·공제 갱신일 알림. 리마인더는 저장하지 않고 조회 시점에 {@code gift} 에서 유도
 * 1. 두 날짜 모두 {@code gift.gift_date} 의 순수 함수라 미리 만들어 둘 이유가 없다.
 * 2. {@code gift} 는 사용자가 수시로 수정·삭제하는 테이블이라 미리 만든 행은 곧 낡는다.
 * 특히 공제 갱신일은 "합산 창 안에서 가장 먼저 빠지는 증여"에 달려 있어 과거 이력 한 건만 추가돼도 바뀐다.
 *
 * <p>앱 밖으로 나가는 발송(푸시·메일)이 붙는 시점에는 이야기가 달라진다."이미 보냈는가"는
 * 유도할 수 없는 상태라 그때는 {@code reminder} 테이블(DDL 에 이미 있다)에 적재해야 한다.
 */

package com.example.project.reminder;


